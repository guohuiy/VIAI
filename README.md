# V8-AI - 高性能计算机视觉部署平台

V8-AI 是一个高性能计算机视觉部署平台，采用插件化架构设计，支持多后端推理引擎，专注于计算机视觉算法的高性能部署与优化。

## 项目特性

- 🚀 **高性能推理**: 支持 TensorRT、ONNX Runtime、OpenVINO、NCNN 多后端
- 🔌 **插件化架构**: 动态加载和管理计算机视觉算法插件
- 💾 **智能内存管理**: 针对 4GB 显存环境优化的内存池系统
- 🧵 **多线程优化**: SIMD 指令集优化和多线程并行处理
- 📊 **性能监控**: 实时性能监控和统计分析
- 🐳 **容器化部署**: 支持 Docker 容器化部署

## 系统架构

```
┌─────────────────────────────────────────────────────┐
│                   应用层 (Application Layer)        │
├─────────────────────────────────────────────────────┤
│               插件管理器 (Plugin Manager)           │
├─────────────────────────────────────────────────────┤
│             统一推理接口 (Inference Engine)          │
├─────────────────────────────────────────────────────┤
│             多后端推理引擎 (Backend Engines)        │
│         TensorRT │ ONNX Runtime │ OpenVINO │ NCNN  │
├─────────────────────────────────────────────────────┤
│               硬件抽象层 (HAL)                     │
│         GPU (CUDA) │ CPU (SIMD) │ 内存管理         │
└─────────────────────────────────────────────────────┘
```

## 快速开始

### 环境要求

- **操作系统**: Windows 10/11, Linux, macOS
- **CUDA**: 11.8+ (NVIDIA GPU)
- **OpenCV**: 4.8+
- **CMake**: 3.20+
- **编译器**: MSVC 2022, GCC 9+, Clang 12+

### 构建项目

```bash
# 克隆项目
git clone <repository-url>
cd V8-AI

# 创建构建目录
mkdir build && cd build

# 配置构建
cmake -DCMAKE_BUILD_TYPE=Release ..

# 编译
make -j$(nproc)

# 运行
./v8_ai
```

### Docker 部署

```bash
# 构建镜像
docker build -t v8-ai:latest .

# 运行容器
docker run --gpus all -v ./models:/app/models -v ./plugins:/app/plugins v8-ai:latest
```

## 插件开发

### 创建自定义插件

```cpp
#include "v8_core/plugin_interface.h"

class CustomDetector : public IPlugin {
public:
    bool initialize(const PluginConfig& config) override {
        // 初始化逻辑
        return true;
    }
    
    PluginResult execute(const PluginInput& input, 
                       PluginOutput& output) override {
        // 自定义处理逻辑
        return PluginResult::Success;
    }
};

// 注册插件
REGISTER_PLUGIN("custom_detector", CustomDetector);
```

### 插件配置

```yaml
# config.yaml
plugins:
  - name: "yolov8_detector"
    type: "detector"
    model: "models/yolov8n.onnx"
    backend: "tensorrt"
    precision: "fp16"
  
  - name: "sam_segmenter"
    type: "segmenter"
    model: "models/sam_base.onnx"
    backend: "onnxruntime"
```

## 性能基准

| 模型 | 后端 | 延迟 (ms) | 显存占用 | FPS |
|------|------|-----------|----------|-----|
| YOLOv8n | TensorRT | 4.2ms | 1.2GB | 238 |
| SAM-Base | ONNX Runtime | 152ms | 3.2GB | 6.5 |
| PaddleOCR | TensorRT | 8.5ms | 0.8GB | 117 |

## 项目结构

```
V8-AI/
├── src/
│   ├── core/              # 核心模块
│   │   ├── plugin_manager.h/cpp    # 插件管理器
│   │   ├── inference_engine.h/cpp  # 推理引擎接口
│   │   └── memory_pool.h/cpp       # 内存池管理
│   ├── backends/          # 推理后端
│   │   ├── tensorrt/      # TensorRT 后端
│   │   ├── onnxruntime/   # ONNX Runtime 后端
│   │   ├── openvino/      # OpenVINO 后端
│   │   └── ncnn/          # NCNN 后端
│   ├── plugins/           # 插件实现
│   │   ├── detectors/     # 检测器插件
│   │   ├── segmenters/    # 分割器插件
│   │   └── classifiers/   # 分类器插件
│   └── utils/             # 工具类
├── include/               # 头文件
├── models/                # 模型文件
├── plugins/               # 插件文件
├── tests/                 # 测试文件
├── docs/                  # 文档
└── CMakeLists.txt         # 构建配置
```

## 贡献指南

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 联系我们

- 项目主页: [https://github.com/your-org/v8-ai](https://github.com/your-org/v8-ai)
- 邮箱: contact@v8-ai.com
- 文档: [docs/](docs/)

## 更新日志

### v1.0.0 (2026-02-04)
- 初始版本发布
- 支持多后端推理引擎
- 插件化架构设计
- 内存优化系统
- 性能监控功能